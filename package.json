{
  "name": "new-hanover-admin",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Administration de New Hanover — plateforme interne (Vercel + Postgres).",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "db:migrate": "psql \"$POSTGRES_URL\" -f ./schema.sql"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
